# ClinResearch AI — المستند التنفيذي للمخرجات الفنية والتكامل المنهجي

هذا المستند يمثل الشرح المنهجي والإداري ودليل التكامل الفني الشامل لجميع المراحل والمخرجات التي تم بناؤها في مشروع **ClinResearch AI**.

---

## 🗺️ المرحلة الأولى: حوكمة المراجع وهيكلة قاعدة المعرفة (Knowledge Base & RAG Setup)

### 1. حوكمة وتنظيم المراجع (126 مرجعاً)
تم دمج وتفصيل **126 مرجعاً أكاديمياً** معتمداً داخل النظام في ملف البرمجة `backend/python_analytics/app/knowledge_catalog.py`. 
* **نوع الدراسة (Study Type):** تم ربط كل مرجع بنوع الدراسة الخاص به (RCT, Prospective, Retrospective, Cross-Sectional, In Vitro, Systematic Review).
* **عزل المراجع (Reference Isolation):** يلتزم محرك الـ RAG والذكاء الاصطناعي برفض استدعاء أي مرجع خارج تصنيفه المحدد لضمان دقة الاسترجاع ومنع تداخل المنهجيات (على سبيل المثال: لا يمكن استخدام معايير STROBE الخاصة بالدراسات الاسترجاعية عند مراجعة تجربة سريرية عشوائية RCT التي تلتزم بمعايير CONSORT).

### 2. هيكلة قاعدة المعرفة ومحرك الـ RAG
* **استراتيجية التقسيم (Chunking Strategy):** يتم تقسيم ملفات البروتوكول والمراجع إلى أجزاء (Chunks) بحجم 500 حرف وتداخل 100 حرف لضمان عدم ضياع السياق الرياضي والمنهجي.
* **الوسم والتصنيف (Metadata Tagging):** يحمل كل جزء وسماً رقمياً يحتوي على:
  - `source_file`: اسم الملف المصدر.
  - `study_type`: نوع الدراسة المربوطة.
  - `evidence_rank`: قوة الدليل العلمي (من A إلى D).
  - `evidence_level_label`: مستوى الدليل العلمي (مثل Level 1a للتحليلات الشمولية).
* **آلية التصفية المعزولة (Metadata Filtering):** قبل تمرير السؤال إلى المساعد، تتم تصفية الـ Chunks ديناميكياً بناءً على نوع الدراسة الحالي لإتاحة المراجع المعتمدة فقط للنموذج.

---

## 🗺️ المرحلة الثانية: بناء المنطق البرمجي وأشجار القرار (Clinical & Statistical Logic)

### 1. التقييم المنهجي واستخراج مقترحات البحث (23 نقطة)
يقوم محرك الذكاء الاصطناعي باستقبال نصوص المقترحات ويمر بثلاث طبقات رئيسية:
1. **طبقة الاستخراج (Extraction Layer):** استخراج 23 عنصراً أساسياً من المقترح (مثل: العنوان، الأهداف، PICO، المتغيرات، نوع الدراسة، الفحص السريري، حجم العينة، التعمية والعشوائية، التحليل الإحصائي، المراجع).
2. **طبقة التحقق المنهجي (Validation Layer):** فحص الترابط المنطقي (مثال: هل تم ذكر تفاصيل العشوائية والتعمية إذا كانت الدراسة مصنفة كـ RCT؟).
3. **طبقة كشف النواقص (Missing Info Detection):** حصر العناصر الغائبة وتنبيه الباحث إليها فوراً في واجهة الاستمارة السريرية.

### 2. مصفوفة التحقق السريري (Clinical Rules & Variable Mapping Matrix)
مبنية في كود `clinical_validation.py` للتحقق من سلامة البيانات المدخلة في العيادة لمنع إدخال بيانات غير منطقية سريرياً:
- **عمر المريض (patientAge):** المدى [0 - 120] سنة.
- **عمق الجيب اللثوي (pocketDepthMm):** المدى [0.0 - 15.0] مم. (تنبيه بالتهاب شديد إذا تجاوزت القيمة 6.0 مم).
- **ضغط الدم (BP):** الانقباضي [50 - 250]، الانبساطي [30 - 150] (تنبيه بالارتفاع إذا تجاوز الانقباضي 140).
- **السكر التراكمي (HbA1c):** المدى [3.0 - 18.0] %.
- **معدل نبضات القلب (Heart Rate):** المدى [30 - 220] نبضة/دقيقة.

### 3. محرك حجم العينة وأشجار القرار (Sample Size Engine)
يحتوي ملف `sample_size_engine.py` على التنفيذ البرمجي للمعادلات الرياضية الإحصائية لـ 9 أنواع من التصاميم البحثية:
* **T-Test (مستقلة / مرتبطة):** $n = 2 \times ((Z_\alpha + Z_\beta) / d)^2$
* **ANOVA و ANOVA للقياسات المتكررة.**
* **اختبار النسب الثنائية (Two-Proportion Z-Test).**
* **تجارب عدم الأدنوية والتكافؤ (Non-Inferiority & Equivalence TOST).**
* **تحليل البقاء (Survival Analysis Log-Rank).**
* **التجارب العنقودية العشوائية (Cluster Randomized Trials) مع تعديل معامل تأثير التصميم (Design Effect - DEFF).**

ويقوم المحرك تلقائياً بـ:
1. **توضيح مبررات الافتراضات:** تفصيل تبرير اختيار قيم $\alpha$، و Power، وحجم الأثر (Effect Size).
2. **تعديل نسبة الانسحاب (Dropout Adjustment):** حساب حجم العينة النهائي بعد التعديل: $N_{\text{adjusted}} = N / (1 - d)$.
3. **تحليل الحساسية (Sensitivity Scenarios):** عرض سيناريوهات بديلة لحجم العينة عند أحجام أثر صغيرة ومتوسطة وكبيرة.

### 4. مصفوفة اختيار الاختبار الإحصائي وتبريره (Statistical Test Selection)
مدمجة في أشجار القرارات البرمجية في `main.py` وتقوم بالاختيار بناءً على:
- نوع المتغير التابع (مستمر، فئوي، ثنائي، وقت حتى الحدث).
- عدد المجموعات المقارنة (مجموعتان، 3 مجموعات أو أكثر).
- استقلالية المجموعات (مستقلة Independent، أو مرتبطة Paired / Repeated Measures).
- اختبار التوزيع الطبيعي وتجانس التباين (فحص التوزيع عبر Shapiro-Wilk وتجانس التباين عبر Levene).
- يقترح المحرك تلقائياً بدائل غير معلمية (Non-parametric) مثل (Mann-Whitney U أو Wilcoxon أو Kruskal-Wallis) في حال عدم تحقق شروط التوزيع الطبيعي.

---

## 🗺️ المرحلة الثالثة: هندسة المحثات، التقارير ودليل التكامل (Prompts & Integration Guide)

### 1. دليل التكامل الموجه للمطورين (Developer Integration Guide)

#### كود معالجة البيانات (FastAPI Python Analytics Server)
يتم تشغيل خادم البايثون الإحصائي كخدمة مستقلة على المنفذ `8000` ويحتوي على المسارات التالية:
* `POST /assistant/chat`: محرك الاستفسارات المنهجية للشات مدعوماً بالـ RAG وعزل المراجع.
* `POST /api/v1/calculate`: لحساب حجم العينة وسيناريوهات الحساسية.
* `POST /analytics/ocr`: لاستخراج النصوص من الصور وتعبئة حقول الفحص اللثوي والفسيولوجي تلقائياً.
* `POST /analytics/validate-clinical`: لتمرير المدخلات السريرية ومقارنتها بمصفوفة قواعد التحقق ونسب الخطورة.

#### كود معالجة الواجهات وخلفية Node.js
تقوم خلفية Node.js باستقبال الطلبات من الواجهة الأمامية (مع التحقق من الهوية وصلاحيات المستخدمين) وتوجيهها مباشرة إلى مسارات خادم البايثون الإحصائي عبر وكيل تمرير مدمج في `analyticsController.ts`.

#### طريقة إضافة مراجع جديدة أو تحديث القواعد
1. **إضافة مرجع جديد:** قم بإضافة كائن جديد داخل مصفوفة `REFERENCE_CATALOG` في ملف `knowledge_catalog.py` مع تحديد المعرف `id` ونوع الدراسة المستهدفة `study_types`.
2. **تعديل قواعد الفحص السريري:** قم بتحديث قيم المدى الأدنى والأقصى في قاموس `RANGE_RULES` بملف `clinical_validation.py`.
3. **إضافة اختبار إحصائي جديد:** قم بإدراج منطق الاختبار ومعادلاته داخل قاموس الموزع `dispatch_map` في ملف `main.py`.

---

## 📦 المخرجات الفنية الجاهزة للتسليم والاستخدام

جميع الملفات البرمجية جاهزة للعمل ومدمجة في مسار المشروع، بالإضافة إلى توليد **الملفات التقنية المنظمة (JSON Data Seeds)** داخل مجلد [deliverables_json](file:///d:/Mostaql/oskar/deliverables_json) لتغذية قاعدة البيانات مباشرة:
1. [knowledge_catalog.json](file:///d:/Mostaql/oskar/deliverables_json/knowledge_catalog.json): المراجع الأكاديمية الـ 126 مع وسوم التصنيف.
2. [clinical_rules_severity.json](file:///d:/Mostaql/oskar/deliverables_json/clinical_rules_severity.json): مصفوفة القياسات الطبية واللثوية المعتمدة ونسب الخطورة.
3. [prompt_library.json](file:///d:/Mostaql/oskar/deliverables_json/prompt_library.json): مكتبة المحثات الحاكمة الموجهة للـ AI.
4. [decision_trees.json](file:///d:/Mostaql/oskar/deliverables_json/decision_trees.json): أشجار قرارات حجم العينة وتصميم الاختبارات الإحصائية.
